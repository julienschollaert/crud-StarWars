{% extends 'base.html.twig' %}

{% block title %}InfoPerso{% endblock %}

{% block body %}
    {% include 'layout/header.html.twig' %}
    <h1>InfoPerso</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ info_perso.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ info_perso.name }}</td>
            </tr>
            <tr>
                <th>Faction</th>
                <td>{{ info_perso.faction }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ info_perso.role }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_info_perso_index') }}">back to list</a>

    <a href="{{ path('app_info_perso_edit', {'id': info_perso.id}) }}">edit</a>

    {{ include('info_perso/_delete_form.html.twig') }}
{% endblock %}
